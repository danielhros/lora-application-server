import React from "react";
import Layout from "./Layout";

export const Devices = () => {
  return (
    <Layout>
      <React.Fragment>Devices</React.Fragment>
    </Layout>
  );
};

export default Devices;
